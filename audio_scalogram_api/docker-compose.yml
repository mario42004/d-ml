services:
  audio-scalogram-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: audio-scalogram-api
    ports:
      - "8001:8001"
    environment:
      APP_ENV: production
      APP_PORT: 8001
